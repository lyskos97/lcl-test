import DefaultLayout from "../layouts/default";
import { withRouter } from "next/router";

const Home = () => {
  return (
    <DefaultLayout>
      <span title="qeq">home</span>
    </DefaultLayout>
  );
};

export default Home;
